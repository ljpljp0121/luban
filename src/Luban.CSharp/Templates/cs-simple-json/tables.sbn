using UnityEngine;

{{namespace_with_grace_begin __namespace}}

public interface ITable
{
    void _LoadData();
}

public interface IJsonTableLoader
{
    string LoadJson(string fileName);
}

public static partial class {{__name}}
{
    private static readonly System.Collections.Generic.Dictionary<System.Type, ITable> tables = new System.Collections.Generic.Dictionary<System.Type, ITable>();
    private static IJsonTableLoader jsonLoader;

    public static void SetJsonLoader(IJsonTableLoader loader)
    {
        jsonLoader = loader;
        Clear();
    }

    public static T GetTable<T>() where T : ITable, new()
    {
        var type = typeof(T);
        if (tables.TryGetValue(type, out var table))
        {
            return (T)table;
        }

        var newTable = new T();
        tables.Add(type, newTable);
        try
        {
            newTable._LoadData();
        }
        catch (System.Exception e)
        {
            tables.Remove(type);
            Debug.LogError($"load table failed. table:{type.FullName}\n{e}");
        }
        return newTable;
    }

    public static void Clear()
    {
        tables.Clear();
    }

    public static string LoadJson(string fileName)
    {
        if (jsonLoader == null)
        {
            throw new System.InvalidOperationException("Json table loader is not set.");
        }
        return jsonLoader.LoadJson(fileName);
    }
}

{{namespace_with_grace_end __namespace}}
